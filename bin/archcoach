#!/usr/bin/env sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
CLI_ARTIFACT="$ROOT_DIR/dist/cli.js"

if [ ! -f "$CLI_ARTIFACT" ]; then
  echo "Missing bundled runtime artifact: dist/cli.js. Run 'bun run build:cli' first." >&2
  exit 1
fi

exec node "$CLI_ARTIFACT" "$@"
